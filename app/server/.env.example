# Copy this file to .env and fill values before starting the server
# Example local MongoDB URL (default port)
MONGODB_URL=mongodb://localhost:27017/pos_db

# JWT secret used to sign access tokens — change to a strong random value in production
JWT_SECRET=change_this_in_env

# Optional server port
PORT=5000
